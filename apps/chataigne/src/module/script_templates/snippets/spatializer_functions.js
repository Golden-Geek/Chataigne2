/*
 * Spatializer module values:
 *   local.values.<target>.<source>
 *
 * The Spatializer module does not expose module-specific script functions yet.
 * Scripts should read the generated target/source value matrix from local.values.
 */
