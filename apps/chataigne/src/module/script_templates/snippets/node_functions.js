/*
 * Node module functions (call them on local):
 *   local.setValue(targetReference, value)
 *   local.trigger(targetReference)
 *
 * References stay stable across save/load and are re-resolved by UUID before
 * every operation.
 */
