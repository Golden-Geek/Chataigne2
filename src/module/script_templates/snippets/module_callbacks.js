function moduleConnectionChanged(param, newValue, oldValue, change) {
  void param;
  void newValue;
  void oldValue;
  void change;
}

function moduleModuleParameterChanged(param, newValue, oldValue, change) {
  void param;
  void newValue;
  void oldValue;
  void change;
}

function moduleModuleValueChanged(param, newValue, oldValue, change) {
  void param;
  void newValue;
  void oldValue;
  void change;
}
