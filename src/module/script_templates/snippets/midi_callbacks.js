function midiMessageReceived(message) {
  void message;
}

function noteOnReceived(channel, note, velocity) {
  void channel;
  void note;
  void velocity;
}

function noteOffReceived(channel, note, velocity) {
  void channel;
  void note;
  void velocity;
}

function ccReceived(channel, controller, value) {
  void channel;
  void controller;
  void value;
}

function sysExReceived(bytes) {
  void bytes;
}
