/*
 * Mouse module functions (call them on local):
 *   local.moveMouse(x, y, coordinate = "absolute", units = "pixels")
 *   local.click(button = "left")
 *   local.pressButton(button = "left")
 *   local.releaseButton(button = "left")
 *   local.scroll(vertical, horizontal = 0)
 *
 * `coordinate` can be "absolute" or "relative".
 * `units` can be "pixels" or "normalized"; normalized values use 0.0-1.0 on the main display and only work with absolute moves.
 */
