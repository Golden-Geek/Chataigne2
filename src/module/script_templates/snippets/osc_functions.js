/*
 * OSC module functions (call them on local):
 *   local.sendMessage(address, ...values)
 *   local.sendOSC(address, ...values)
 *   local.sendOsc(address, ...values)
 *
 * The address must be a non-empty OSC path string. Values may be numbers, strings,
 * booleans, vec2/vec3 arrays, or color arrays; vector and color values expand into
 * multiple OSC arguments.
 */

