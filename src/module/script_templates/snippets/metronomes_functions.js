/*
 * Metronomes module functions (call them on local):
 *   local.resetMetronomes()
 *   local.resetMetronome(nameOrIndex)
 *   local.tickMetronome(nameOrIndex)
 *
 * `nameOrIndex` may be the metronome label or a 1-based index.
 */
