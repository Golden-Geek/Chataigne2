/*
 * Metronomes module callbacks:
 *   metronomeTick(name, ticks, totalTicks, details)
 */
function metronomeTick(name, ticks, totalTicks, details) {
  void name;
  void ticks;
  void totalTicks;
  void details;
}
