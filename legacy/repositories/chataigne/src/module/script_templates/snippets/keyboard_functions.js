/*
 * Keyboard module functions (call them on local):
 *   local.tapKey(key = "space")
 *   local.pressKey(key)
 *   local.releaseKey(key)
 *
 * Use supported physical key ids such as "a", "space", "enter", "left_control", or "numpad_1".
 */