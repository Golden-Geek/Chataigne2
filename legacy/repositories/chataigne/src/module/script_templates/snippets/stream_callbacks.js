function textReceived(text, source) {
  void text;
  void source;
}

function dataReceived(bytes, source) {
  void bytes;
  void source;
}

function dataReceive(bytes, source) {
  void bytes;
  void source;
}
